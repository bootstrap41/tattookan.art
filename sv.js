// PWA yükleme kriterini karşılamak için basit bir servis işçi yapısı
self.addEventListener('install', (e) => {
	  self.skipWaiting();
	  });

	  self.addEventListener('fetch', (e) => {
	  	  // Sitenin stabil çalışması için istekleri normal şekilde iletir
	  	  });
	  })
})
